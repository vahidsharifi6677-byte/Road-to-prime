# Road to Prime — Home Screen Shortcut Ready

Open `index.html` in a browser that supports "Add to Home screen".
For the most app-like experience, host this folder on HTTPS and use the browser's
Install/Add to Home screen option.

The app keeps its local progress using browser storage.
